cat > .env.example << 'EOF'
# Copy this file to .env and fill in values if switching providers
# Open-Meteo requires no key — leave blank for default behavior
WEATHER_API_KEY=
WEATHER_API_BASE_URL=https://api.open-meteo.com/v1/forecast
EOF